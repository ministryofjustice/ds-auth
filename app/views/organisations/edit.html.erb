<%= render 'shared/error_messages', object: @organisation %>

<h1><%= t('edit_organisation') %></h1>

<%= form_for(@organisation) do |f| %>
  <%= render 'form', f: f %>

  <div class="actions">
    <%= f.submit t('update_organisation'), class: "button" %>
  </div>

<%- end %>

<%= link_to t('back'), organisations_path %>
